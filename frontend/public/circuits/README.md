# Circuit Artifacts

Browser proof artifacts are generated by `scripts/generate-zk-artifacts.mjs`.

This directory intentionally does not contain checked-in placeholder `.wasm`,
`.zkey`, or verification-key files. Generate artifacts only from the current
Circom sources and a documented Powers of Tau input.
