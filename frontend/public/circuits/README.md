# Circuit Artifacts

Browser proof artifacts are generated by `scripts/generate-zk-artifacts.mjs`.

Current contents are DEV/TEST artifacts generated from the local circuits and
the non-production `circuits/keys/dev_pot14_final.ptau` setup.

Do not treat these files as production trusted-setup artifacts. Regenerate
production artifacts only from reviewed ceremony material with documented
provenance and hashes.
